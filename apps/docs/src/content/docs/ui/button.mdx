---
title: Button
description: An interactive button component with multiple variants and sizes.
---

import { ComponentPreview } from '../../../components/react/ComponentPreview';
import { InstallTabs } from '../../../components/react/InstallTabs';
import { PropsTable } from '../../../components/react/PropsTable';
import ButtonDemo from '../../../components/demos/ui/button-demo';
import ButtonDemoSource from '../../../components/demos/ui/button-demo.tsx?raw';
import ButtonVariants from '../../../components/demos/ui/button-variants';
import ButtonVariantsSource from '../../../components/demos/ui/button-variants.tsx?raw';
import ButtonSizes from '../../../components/demos/ui/button-sizes';
import ButtonSizesSource from '../../../components/demos/ui/button-sizes.tsx?raw';
import ButtonWithIcon from '../../../components/demos/ui/button-with-icon';
import ButtonWithIconSource from '../../../components/demos/ui/button-with-icon.tsx?raw';

A versatile button component that supports different styles, sizes, and behaviors.

<ComponentPreview code={ButtonDemoSource} client:only="react">
  <ButtonDemo client:only="react" />
</ComponentPreview>

## Installation

<InstallTabs packages="@rhinolabs/ui" client:only="react" />

## Usage

```tsx
import { Button } from "@rhinolabs/ui"

export default function Example() {
  return <Button variant="default">Click me</Button>
}
```

## Examples

### Variants

Buttons come in different variants to suit different contexts.

<ComponentPreview code={ButtonVariantsSource} client:only="react">
  <ButtonVariants client:only="react" />
</ComponentPreview>

### Sizes

Buttons are available in different sizes to fit your design needs.

<ComponentPreview code={ButtonSizesSource} client:only="react">
  <ButtonSizes client:only="react" />
</ComponentPreview>

### With Icons

Buttons can include icons alongside text.

<ComponentPreview code={ButtonWithIconSource} client:only="react">
  <ButtonWithIcon client:only="react" />
</ComponentPreview>

## API Reference

<PropsTable
  data={[
    { name: "variant", type: '"default" | "destructive" | "outline" | "secondary" | "ghost" | "link"', default: '"default"', description: "The visual style of the button" },
    { name: "size", type: '"default" | "sm" | "lg" | "icon"', default: '"default"', description: "The size of the button" },
    { name: "asChild", type: "boolean", default: "false", description: "Render as child component using Radix Slot" },
  ]}
  client:only="react"
/>

The button component also accepts all standard HTML button attributes.

## Accessibility

- Supports keyboard navigation
- Includes proper ARIA attributes
- Maintains focus states
- Provides clear visual feedback
- Handles disabled states correctly
